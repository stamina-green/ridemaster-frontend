import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.stamina.transportation.ridemaster',
  appName: 'RideMaster App',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
